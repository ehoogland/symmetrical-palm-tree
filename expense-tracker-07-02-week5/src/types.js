/**
 * @typedef {Object} Expense
 * @property {number} id
 * @property {string} merchant
 * @property {string} [description]
 * @property {number|string} amount
 * @property {string} [transactionDate] ISO date string (YYYY-MM-DD)
 */

/**
 * @typedef {Array<Expense>} ExpensesList
 */

/**
 * @typedef {Expense | null} MaybeExpense
 */
